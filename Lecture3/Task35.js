function tree(n){
    let baseLine=" | ";
    const firstLineCount=baseLine.length;
    for(let i=0; i<=n; i++){
        let line = "";
        for(let j=0; j<(2*n + firstLineCount); j++){


        }
    }
}

tree(1);
tree(3);
tree(4);
tree(7);